# Run Scripts

This folder is for scripts that start local project services.

`run_godot_ui_dev.sh` opens the Godot LCD UI prototype as a fixed 640x480 development window on the current HDMI monitor.

`run_godot_ui_lcd.sh` is only a placeholder for future fullscreen LCD mode. It does not launch fullscreen in this sprint.
