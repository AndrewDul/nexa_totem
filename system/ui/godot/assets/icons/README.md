# Icons

This folder is reserved for small local icon assets.

Use it only for lightweight icons that render reliably on the LCD. Do not rely on emoji icons for critical UI, because some emoji glyphs render poorly on the Raspberry Pi display.

Current message and notification indicators are drawn directly with Godot primitives in `main.gd`.
