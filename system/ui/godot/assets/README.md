# Godot Assets

This folder is for lightweight local UI assets used by the Godot LCD interface.

Prefer simple, low-memory assets that work well on Raspberry Pi 5 2GB. Do not add large images, shaders, or heavy visual effects.

Most current icons are custom-drawn in `main.gd`; assets can be added later when they are truly needed.
