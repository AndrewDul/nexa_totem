# Data

This folder is prepared for lightweight local UI data during development.

The prototype does not include generated reports here. Future code may copy selected saved diagnostic reports into this folder or read them through the Python backend.

